using UnityEngine;
using System.Collections;

public class UpgradePlayerController : MonoBehaviour {

 //   public float speed;
 //   public float range;

 //   private Rigidbody rb;

	//// Use this for initialization
	//void Start () {
 //       rb = GetComponent<Rigidbody>();
	//}
	
	//// Update is called once per frame
	//void Update ()
 //   {
 //       if (UpgradeSceneController.instance.SceneState != UpgradeSceneController.States.Intro)
 //       {
 //           float moveHorizontal = Input.GetAxis("Horizontal");
 //           rb.velocity = new Vector3(moveHorizontal, 0, 0) * speed;
 //           rb.position = new Vector3()
 //           {
 //               x = Mathf.Clamp(rb.position.x, -range, range),
 //               y = rb.position.y,
 //               z = rb.position.z
 //           };
 //       }
 //   }
}
